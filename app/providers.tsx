'use client';import { OnchainKitProvider } from '@coinbase/onchainkit';import { base, baseSepolia } from 'viem/chains';import { ReactNode } from 'react';const API_KEY=process.env.NEXT_PUBLIC_CDP_API_KEY as string;const PAYMASTER_URL=process.env.NEXT_PUBLIC_PAYMASTER_AND_BUNDLER_ENDPOINT as string;const CHAIN_ID=Number(process.env.NEXT_PUBLIC_CHAIN_ID||84532);const chain=CHAIN_ID===8453?base:baseSepolia;export default function Providers({children}:{children:ReactNode}){return(<OnchainKitProvider apiKey={API_KEY} chain={chain} config={{paymaster:PAYMASTER_URL}}>{children}</OnchainKitProvider>);}