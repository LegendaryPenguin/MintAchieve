import './styles/globals.css';import type { Metadata } from 'next';import { APP } from '@/src/config';import Providers from './providers';export const metadata:Metadata={title:APP.name,description:APP.description};export default function RootLayout({children}:{children:React.ReactNode}){return(<html lang='en'><body><Providers>{children}</Providers></body></html>);}